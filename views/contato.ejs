<%- include('partials/header', { title: 'Contato' }) %>

    <main class="main-container">
        <section class="content-full">
            <div class="section-header">
                <h2 class="section-title scroll-reveal">Entre em Contato</h2>
            </div>

            <div class="form-container scroll-reveal" style="max-width: 600px; margin: 0 auto;">
                <p style="margin-bottom: 2rem; color: var(--text-muted);">Tem alguma dúvida, sugestão ou quer enviar uma
                    pauta? Preencha o formulário abaixo.</p>

                <form action="#" method="POST" class="contact-form">
                    <div style="margin-bottom: 1.5rem;">
                        <label for="nome" style="display: block; margin-bottom: 0.5rem;">Nome</label>
                        <input type="text" id="nome" name="nome"
                            style="width: 100%; padding: 1rem; background: var(--surface-color); border: 1px solid var(--border-color); color: var(--text-main);"
                            required>
                    </div>

                    <div style="margin-bottom: 1.5rem;">
                        <label for="email" style="display: block; margin-bottom: 0.5rem;">Email</label>
                        <input type="email" id="email" name="email"
                            style="width: 100%; padding: 1rem; background: var(--surface-color); border: 1px solid var(--border-color); color: var(--text-main);"
                            required>
                    </div>

                    <div style="margin-bottom: 1.5rem;">
                        <label for="mensagem" style="display: block; margin-bottom: 0.5rem;">Mensagem</label>
                        <textarea id="mensagem" name="mensagem" rows="5"
                            style="width: 100%; padding: 1rem; background: var(--surface-color); border: 1px solid var(--border-color); color: var(--text-main);"
                            required></textarea>
                    </div>

                    <button type="button" class="post-card"
                        style="width: 100%; color: var(--text-main); cursor: pointer; background: transparent; padding: 1rem; text-transform: uppercase;">Enviar
                        Mensagem</button>
                </form>
            </div>
        </section>
    </main>

    <%- include('partials/footer') %>