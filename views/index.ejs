<%- include('partials/header') %>

    <main class="main-container">
        <section class="posts-feed" aria-label="blog posts">
            <div class="section-header">
                <h2 class="section-title scroll-reveal">Últimas Publicações</h2>
            </div>

            <% posts.forEach(post=> { %>
                <article class="post-card scroll-reveal">
                    <div class="post-meta">
                        <span class="category <%= post.categoryClass %>">
                            <%= post.category %>
                        </span>
                        <span class="date">
                            <%= post.date %>
                        </span>
                    </div>
                    <h3 class="post-title"><a href="#">
                            <%= post.title %>
                        </a></h3>
                    <p class="post-excerpt">
                        <%= post.excerpt %>
                    </p>
                    <div class="post-footer">
                        <span class="author">por <%= post.author %></span>
                        <a href="/artigo/<%= post.id %>" class="read-more"><span>Ler artigo</span> <span
                                class="arrow">→</span></a>
                    </div>
                </article>
                <% }); %>

        </section>

        <aside class="sidebar scroll-reveal">
            <div class="widget author-widget">
                <h3 class="widget-title">O Cronista do Abismo</h3>
                <p class="author-bio">Navegando pelos escombros da civilização. Aqui, a esperança é uma moeda
                    falsificada e a verdade, uma ferida aberta.</p>
            </div>

            <div class="widget categories-widget">
                <h3 class="widget-title">Facções da Ruína</h3>
                <ul class="category-list">
                    <li><a href="#" class="cat-link"><span>Esquerda</span> <span class="count">(Morto)</span></a></li>
                    <li><a href="#" class="cat-link"><span>Direita</span> <span class="count">(Podre)</span></a></li>
                    <li><a href="#" class="cat-link"><span>Centro</span> <span class="count">(Nulo)</span></a></li>
                    <li><a href="#" class="cat-link"><span>O Esgoto</span> <span class="count">(☣️)</span></a></li>
                    <li><a href="#" class="cat-link"><span>O Futuro Perdido</span> <span class="count">(∅)</span></a>
                    </li>
                </ul>
            </div>

            <div class="widget search-widget">
                <form class="search-form">
                    <input type="text" class="search-input" placeholder="Buscar nos escombros...">
                    <button type="submit" class="search-btn">👁️</button>
                </form>
            </div>
        </aside>
    </main>

    <%- include('partials/footer') %>